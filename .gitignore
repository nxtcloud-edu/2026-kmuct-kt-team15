# LLM endpoint and API key.
.env

# The submission repo does not carry data (SPEC section 12): the prepared DB and the
# crawl live only on disk and on the server.
data/

__pycache__/
.venv/

# Local worktrees from lane runs.
.claude/worktrees/

# pytest tmp_path root (pytest.ini) and cache.
.pytest_tmp/
.pytest_cache/

# Release tarball built by scripts/release.sh.
dist/
